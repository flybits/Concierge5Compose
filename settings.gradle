pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven {
            url "https://flybits.jfrog.io/artifactory/android-repo/"
        }
        maven {
            url "https://flybits.jfrog.io/artifactory/v3/"
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://flybits.jfrog.io/artifactory/android-repo/"
        }
        maven {
            url "https://flybits.jfrog.io/artifactory/v3/"
        }
    }
}
rootProject.name = "Concierge5 Compose"
include ':app'
